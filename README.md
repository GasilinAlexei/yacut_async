# YaCut Async

YaCut Async — улучшенная версия сервиса сокращения ссылок YaCut, дополненная
возможностью **асинхронной загрузки файлов на Яндекс.Диск** и автоматической
генерацией коротких ссылок для скачивания этих файлов.

Проект использует:
- Flask
- SQLAlchemy
- Асимметричную загрузку через aiohttp
- Docker & GitHub Actions (CI/CD)
- Валидаторы форм и API согласно ТЗ и OpenAPI

---

## Функциональность

### Укорачивание URL
- Пользователь вводит длинную ссылку.
- Может указать свой вариант короткой ссылки (до 16 символов).
- Или сервис сгенерирует автоматически (латиница + цифры).
- Запрещено использовать уже занятые короткие ссылки.
- Запрещено занимать `/files`.

### Асинхронная загрузка файлов на Яндекс.Диск
- На странице `/files` можно загрузить **несколько файлов одновременно**.
- Загрузка выполняется **асинхронно** с помощью `aiohttp`.
- Для каждого файла создаётся короткая ссылка.
- Если короткая ссылка уже существует — генерируется новая.

### Переадресация
Переход по `/<short>` ведет:
- либо на оригинальный URL,
- либо на ссылку для скачивания файла.

### API
Доступны 2 эндпоинта:

POST /api/id/ # создать короткую ссылку
GET /api/id/<id>/ # получить оригинальную ссылку

---

## Стек технологий

- Python 3.9  
- Flask 3  
- SQLAlchemy  
- WTForms  
- aiohttp  
- Docker / Docker Hub  
- GitHub Actions (CI/CD)  
- Yandex Disk API  

---

# Установка и запуск локально

### 1. Клонирование репозитория

```bash
git clone git@github.com:GasilinAlexei/yacut_async.git
cd yacut_async
```
### 2. Создание виртуального окружения
```bash
Копировать код
python -m venv venv
source venv/bin/activate   # Linux / macOS
venv\Scripts\activate      # Windows
```
### 3. Установка зависимостей
```bash
Копировать код
pip install -r requirements.txt
```
### 4. Создание файла .env

Создайте файл .env в корневой директории:

```bash
ini
Копировать код
FLASK_APP=yacut_async
FLASK_DEBUG=1
DATABASE_URI=sqlite:///db.sqlite3
SECRET_KEY=your_secret_key
DISK_TOKEN=your_yandex_disk_token
```

### 5. Инициализация базы

```bash
Копировать код
flask db upgrade
```

### 6. Запуск проекта

```bash
Копировать код
flask run
Теперь вы можете открыть сайт:

http://127.0.0.1:5000/
```

# Запуск через Docker

### 1. Сборка контейнера

```bash
Копировать код
docker build -t yacut_async .
```

### 2. Запуск docker-compose

```bash
Копировать код
docker-compose up --build
CI/CD (GitHub Actions)
Workflow выполняет:

Запуск тестов (pytest).

Сборку Docker-образа.

Публикацию образа в Docker Hub.

Отправку Telegram-уведомления.

Пример уведомления:
bash
Копировать код
🚀 Деплой успешно выполнен!
Репозиторий: username/yacut_async
Коммит: <hash>
Ссылка: https://github.com/username/yacut_async/commit/<hash>
```

## Автор
GitHub: [GasilinAlexei](https://github.com/GasilinAlexei)
